@model CapaAdmin.Models.TRANSACCION

@{
    ViewBag.Title = "Editar la Trasaccion";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var btnZise = "h4";
    var btnPadding = "pt-1 pb-1";
}

<div class="card mt-5 border-dark">
    <div class="card-header bg-dark bg-gradient">
        <h4 class="text-center text-white">
            @ViewBag.Title
        </h4>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">

            <div class="form-group m-3 ">
                @Html.LabelFor(model => model.TIPO_TRANSACCION, htmlAttributes: new { @class = "control-label col-md-2 fw-bold mb-1" })
                <div class="col-md-12 m-1">
                    @Html.EditorFor(model => model.TIPO_TRANSACCION, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.TIPO_TRANSACCION, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group card-footer">
                <div class="col-md-offset-2 col-md-10">
                    <a class="btn btn-primary @btnZise @btnPadding" href="@Url.Action("Index", "TRANSACCIONs")"><i class="bi bi-arrow-bar-left"></i></a> 
                    <input type="submit" value="Guardar" class="btn btn-success @btnZise @btnPadding" />
                </div>
            </div>
        </div>
    }
</div>