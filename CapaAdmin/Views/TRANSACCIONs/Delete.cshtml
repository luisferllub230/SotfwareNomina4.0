@model CapaAdmin.Models.TRANSACCION

@{
    ViewBag.Title = "Borra transaccione";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var btnZise = "h4";
    var btnPadding = "pt-1 pb-1";
}

<div class="card mt-5 border-dark">
    <div class="card-header bg-dark bg-gradient">
        <h4 class="text-center text-white">
            @ViewBag.Title
        </h4>
    </div>

    <div>
        <dl class="dl-horizontal m-3">
            <dt>
                @Html.DisplayNameFor(model => model.TIPO_TRANSACCION)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.TIPO_TRANSACCION)
            </dd>


        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color card-footer">
                <a class="btn btn-primary @btnZise @btnPadding" href="@Url.Action("Index", "TRANSACCIONs")"><i class="bi bi-arrow-bar-left"></i></a>
                <input type="submit" value="Borrar" class="btn btn-success @btnZise @btnPadding" />
            </div>
        }
    </div>
</div>