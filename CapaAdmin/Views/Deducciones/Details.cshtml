@model CapaAdmin.Models.TP_DEDUCCIONES

@{
    ViewBag.Title = "Detalles";
    Layout = "~/Views/Shared/_Layout.cshtml";
}




<div class="card mt-5 border-dark">
    <div class="card-header bg-dark bg-gradient">
        <h4 class="text-center text-white">
            @ViewBag.Title
        </h4>
    </div>

    <div class="m-3">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.NOMBRE)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.NOMBRE)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DEPENDE_SALARIO)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DEPENDE_SALARIO)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ESTADO)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ESTADO)
            </dd>

        </dl>
    </div>

    <div class="card-footer">
        <a href="@Url.Action("Index", "Deducciones")" class="btn btn-primary"><i class="bi bi-arrow-bar-left"></i></a>
        <a href="@Url.Action("Edit", "Deducciones", new {id = Model.ID_TPDEDU})" class="ms-1 btn btn-warning"><i class="bi bi-pencil-square"></i></a>   
    </div>
</div>